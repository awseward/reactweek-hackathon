wordTransformationUtil = {
  // Some silly, contrived examples ==========================================
  upperCase: (word) => {
    return word.toUpperCase();
  },

  lowerCase: (word) => {
    return word.toLowerCase();
  }
  // =========================================================================
};

module.exports = wordTransformationUtil;
